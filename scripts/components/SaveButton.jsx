import React from 'react'
import { connect } from 'react-redux'

let SaveButton = React.createClass({
    render: function () {
        return (
            <button className="button">Save</button>
        )
    }
})

export default SaveButton
